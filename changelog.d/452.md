### Changed

- **产文档角色卡改为 kg-first authoring 契约** —— product-manager / architect / ui-designer / tech-lead / qa-engineer / devops 六张卡的 Output Contract 不再指示直接产出 `docs/<doc>.md`，改为经 `cataforge context` authoring 落稿(`context write-doc` / `write-narrative` / `transact`)+ `cataforge context finalize` 导出人审 markdown 视图;markdown 后端仍按模板实例化后编辑文件。architect 卡不再把分卷(API/DATA/模块)硬编进提示词——拆卷交由 finalize 按导出布局决定。

### Added

- **`check_doc_authoring_invariant` 守卫** —— 锁定上述契约:实例化文档模板的角色卡若其 Output Contract 未经 `finalize` 导出即视为 markdown-first 回归而失败;接入 `run_local.py` 与 pre-commit,逃生标记 `<!-- allow-doc-authoring: <reason> -->`。

### Removed

- **图本体死 schema `cf:Volume`** —— `cf:Volume` 类及 `has_volume` / `part_of_volume` / `volume_type` slot 声明却从不被 ingest 生产、export 消费,予以移除。图结构收敛为单一逻辑 `cf:Document → cf:Section`,拆分为物理分卷文件成为导出布局职责而非图节点。
