"""Package for shipped e2e backdoor / real-input rules YAMLs.

Files here are the default rule set. Per-project overrides go to
``.cataforge/skills/testing/rules/`` and replace the package default
for the same ``(rule_type, language)`` pair.
"""
