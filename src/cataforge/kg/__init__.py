"""Knowledge graph package — sub-PR 1 placeholder.

Runtime modules land in sub-PR 2 (`store`, `config`) and onward; this
file only marks the package so `src/cataforge/kg/schemas/` is importable
via `importlib.resources` for the codegen script.
"""
