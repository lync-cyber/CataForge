"""LinkML schema source files — single source of truth for the KG layer.

Use `importlib.resources.files("cataforge.domain.kg.schemas")` to resolve the
YAML paths at runtime (codegen, ingest bootstrap, store init).
"""
