"""Package for shipped wiring rules YAMLs (code-review skill).

Files here are the default rule set. Per-project overrides go to
``.cataforge/skills/code-review/rules/`` and replace the package
default for the same ``(rule_type, language)`` pair.
"""
